# Binaries for programs and plugins
*.exe
*.dll
*.so
*.dylib

# Test binary, build with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

gitflow-release
.gitflow-release.yaml

# Vendor related
vendor
Gopkg.lock
